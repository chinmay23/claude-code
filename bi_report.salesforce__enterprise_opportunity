create view bi_report.salesforce__enterprise_opportunity as (
select
enterprise_id
,enterprise_name
,amount
,opportunity_name
,amount
from
source.salesforce_opportunity so
left join
source.salesforce_enterprise se
on
so.opportunity_id=se.id
)
;
